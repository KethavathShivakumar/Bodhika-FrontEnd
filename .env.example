# Bodhika Assessment Suite - Environment Configuration
# This frontend is fully client-side and does not require any mandatory environment variables to build or run.
# If connecting to an external backend API in the future, specify the endpoint below:

# VITE_API_BASE_URL=https://api.bodhika.example.com
